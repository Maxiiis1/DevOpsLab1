services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: go-multistage-deps-app
    ports:
      - "8080:8080"
    environment:
      APP_NAME: "Go MultiStage Deps"
      APP_PORT: 8080
    restart: unless-stopped
